(function () {
    'use strict';

    angular
        .module('pstApp')
        .controller('needHelpCtrl', needHelpCtrl);

    needHelpCtrl.$inject = ['$scope', '$uibModalInstance'];
    /* @ngInject */
    function needHelpCtrl($scope, $uibModalInstance) {
        var self = this;

        self.close = function () {
            $uibModalInstance.dismiss();
        };
    }
})();